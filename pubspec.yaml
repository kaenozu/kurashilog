name: kurashilog
description: "くらしログ - 位置履歴から生活の変化を見つけるオフライン分析アプリ"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  drift: ^2.28.0
  sqlite3_flutter_libs: ^0.5.33
  path_provider: ^2.1.5
  path: ^1.9.1
  intl: ^0.20.2
  collection: ^1.19.1
  url_launcher: ^6.3.2
  crypto: ^3.0.7
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.28.0
  build_runner: ^2.4.16

flutter:
  uses-material-design: true
